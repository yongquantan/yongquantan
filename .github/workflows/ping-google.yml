name: Ping Google Sitemap

on:
  push:
    branches: [main]

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Google sitemap
        run: curl -s "https://www.google.com/ping?sitemap=https://yongquantan.com/sitemap.xml"
